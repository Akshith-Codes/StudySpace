import {
  DEPARTMENTS,
  YEARS,
  SPACE_TYPES,
  LOCATIONS,
  FACILITIES,
  NOISE_LEVELS,
  AVAILABILITY_LEVELS,
  SEAT_TYPES,
  SEAT_STATES,
  BOOKING_STATUSES,
  ISSUE_TYPES,
  ISSUE_STATUSES,
  RATING_CATEGORIES,
} from './constants'

export const asConst = (arr) => arr

export const departments = asConst(DEPARTMENTS)
export const years = asConst(YEARS)
export const spaceTypes = asConst(SPACE_TYPES)
export const locations = asConst(LOCATIONS)
export const facilities = asConst(FACILITIES)
export const noiseLevels = asConst(NOISE_LEVELS)
export const availabilityLevels = asConst(AVAILABILITY_LEVELS)
export const seatTypes = asConst(SEAT_TYPES)
export const seatStates = asConst(SEAT_STATES)
export const bookingStatuses = asConst(BOOKING_STATUSES)
export const issueTypes = asConst(ISSUE_TYPES)
export const issueStatuses = asConst(ISSUE_STATUSES)
export const ratingCategories = asConst(RATING_CATEGORIES)
